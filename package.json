{
  "name": "mst-ws-domain",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "tsc",
    "build": "vite build",
    "prepublish": "npm run build",
    "publish": "cd ./dist && npm link"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
